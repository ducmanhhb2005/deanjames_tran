{
  "name": "deanjames-tran-cosmic-web-no-db",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack cosmic artist website for Deanjames Tran using React, Node.js and Express with static API data.",
  "engines": {
    "node": ">=20"
  },
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace server\" \"npm run dev --workspace client\"",
    "build": "npm run build --workspace client",
    "start": "npm run start --workspace server",
    "setup": "npm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
